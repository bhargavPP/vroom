@model int
<td class="text-end">
    <div class="btn-group" role="group">
        <a type="button" class="btn btn-primary" href="@Url.Action("Edit","Model",new { Id = Model })"><i class="fas fa-edit"></i>Edit</a>
        <form method="post" asp-action="Delete">
            <button type="submit" onclick="if(!confirm('Are you sure want to Delete?')) { return false}" class="btn btn-danger" asp-route-id="@Model" asp-action="Delete"><i class="fas fa-trash-alt"></i>Delete </button>
        </form>
        @*<a type="button" class="btn btn-primary" asp-action="Delete" asp-controller="Model" asp-route-id="@item.Id">Delete</a>*@
        @*@Html.ActionLink("Delete", "Model", new {@id = item.Id,@class = "ui-button", style = "background-color:red;" ,onClick = "return confirm('do delete');"})*@
    </div>
</td>